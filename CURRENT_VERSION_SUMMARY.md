# 猪咪备忘录 4.3.7 版本摘要

## 版本定位

猪咪备忘录是一款本地运行的 Windows 剪贴板历史与临时编辑工具，使用 Python、PySide6 和 SQLite 开发。程序支持文本、图片和文件三类剪贴板记录，数据仅保存在本机，并兼容 Clipboard Plus V1 已有历史数据。

当前 Windows 版本为 **4.3.7**，默认安装位置为：

`%LOCALAPPDATA%\Programs\ZhumiMemo\`

## 核心功能

- 自动监听并持久化文本、图片和文件剪贴板，重复内容再次复制时自动移动到顶部。
- 文本、图片、文件和即时编辑台四个独立模块，切换时带轻量淡入淡出动画和对应柴郡主题背景。
- 约 170ms 的模块切换采用可中断状态机；快速连续点击会合并为最后一次选择，不重复刷新中间模块，也不会残留透明效果。
- 默认使用 `Alt + V` 强制打开、恢复并置前，可在设置中自定义全局快捷键。
- 任务栏按钮支持标准最小化与恢复；最小化后任务栏图标保留。`Esc`、关闭按钮或托盘操作用于隐藏到系统托盘。
- 顶部实时搜索支持内容、备注和来源程序，可与今天、昨天、最近 7 天、最近 30 天及自定义日期范围组合筛选。
- 支持键盘方向键选择，`Enter` 复制并隐藏；双击文本或图片打开完整预览，双击文件直接打开。
- 文本和图片可添加备注，非空备注会自动收藏；右键可复制备注，或同时复制备注与原文本/图片。
- 支持收藏、固定、自定义分类、分类重命名和删除，以及将记录移动到系统分类或自定义分类。
- URL 可直接使用浏览器打开或复制域名；路径和文件可打开目标、所在文件夹或另存为。
- 尽可能记录复制来源程序，并提供更完整的文本、图片和文件右键菜单。

## 图片与文件缓存

- 普通图片历史上限可设置为 5—200 张；收藏和固定图片不计入自动清理范围。
- 删除图片记录时同步删除对应本地缓存，避免残留垃圾文件。
- 大图预览支持滚轮缩放、拖动、原始大小和适应窗口，读取与解码在后台执行。
- 文件在后台分块缓存，避免大文件阻塞主界面。
- 文件缓存容量可设置为 128MB—2048MB；超限后优先清理最旧且未收藏、未固定的文件记录。
- 可设置允许缓存的文件扩展名；常见图片文件会自动归入图片历史。

## 即时编辑台

“编辑”模块可自由输入文本，并粘贴、选择或拖入图片。支持复制文本、复制图片、同时复制文本与图片以及一键清空。编辑内容只保留在当前运行会话中，不写入剪贴板历史数据库。

## 设置与系统集成

- 自定义全局快捷键。
- 设置最大文本数量、普通图片数量和文件缓存容量。
- 设置可缓存文件格式。
- 设置内容面板透明度，范围为 0%—100%。
- 修改数据存放位置，并安全迁移数据库、图片和文件缓存。
- 显示文本、图片、文件以及总缓存占用空间。
- 设置 Windows 登录后自动启动。
- 托盘菜单支持打开、暂停或恢复记录、清空历史、设置和退出。
- 只允许运行一个程序实例。

## 数据位置

默认数据目录为：

`%LOCALAPPDATA%\ClipboardPlus\`

主要内容包括：

- `clipboard.db`：SQLite 历史数据库与设置。
- `images\`：图片缓存。
- `files\`：文件缓存。

如果用户在设置中修改了数据存放位置，则以上内容位于所选目录；迁移完成后旧目录会保留为安全备份。

## 界面与窗口行为

界面沿用柴郡/碧蓝航线风格，采用天蓝、淡蓝、蓝白渐变玻璃背景，并使用本地立绘、水彩晕染和半透明笔触。主窗口支持自绘标题栏、四边和四角缩放、最大化、固定窗口及 DPI 缩放。

窗口行为如下：

- 点击任务栏图标：打开、恢复或将当前前台窗口标准最小化。
- `Alt + V`：始终打开、恢复并置前，不执行切换隐藏。
- `Esc` 或关闭按钮：隐藏到托盘，程序继续监听剪贴板。
- 托盘图标：根据窗口状态打开、置前或隐藏。
- 窗口隐藏或最小化会取消尚未执行的置前重试，避免旧计时器重新抢占焦点或恢复窗口。

## 开发与发布状态

- Windows 安装包：`release/ZhumiMemo-Setup-4.3.7.exe`
- 完整自动化测试：84 项通过。
- 安装版已在本机覆盖安装并恢复运行。
- 项目包含 Android 前台原型和构建环境说明，但当前正式交付仍以 Windows 安装版为主。
